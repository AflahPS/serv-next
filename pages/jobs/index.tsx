import React from "react";
import { Layout } from "../../components/common";
import { LoadingCard } from "../../ui";

const Jobs = () => {
  return (
    <Layout>
      <LoadingCard />
    </Layout>
  );
};

export default Jobs;
