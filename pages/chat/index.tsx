import React from "react";
import { Layout } from "../../components/common";
import { LoadingCard } from "../../ui";

const Chat = () => {
  return (
    <Layout>
      <LoadingCard />
    </Layout>
  );
};

export default Chat;
